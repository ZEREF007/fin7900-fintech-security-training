# Dependencies
node_modules/
client/node_modules/

# Environment variables
.env
.env.local
.env.production

# Database (local only — Render uses ephemeral storage)
training.db
training.db-shm
training.db-wal
*.db-shm
*.db-wal

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Build output (Render runs the build command)
# public/   ← keep commented out so local builds persist

# Editor
.vscode/settings.json
.vscode/launch.json

# Python
.venv/

# Local working docs
*.md
!README.md
__pycache__/
*.pyc

# Assignment brief (local only)
extra/

# Local video files (served via YouTube instead)
Videos/

# Keep PPTX/ tracked so Render can serve it

# Logs
*.log
npm-debug.log*
