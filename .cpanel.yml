---
deployment:
  tasks:
    - export DEPLOYPATH=/home/guarfwvk/GuardYourData/
    - /bin/cp -r . $DEPLOYPATH
    - cd $DEPLOYPATH && source /home/guarfwvk/nodevenv/GuardYourData/20/bin/activate && npm install --production && npm rebuild better-sqlite3
